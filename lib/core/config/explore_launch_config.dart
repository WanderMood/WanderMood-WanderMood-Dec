/// Product toggle: keep Explore anchored to one city (no city picker / no drift).
///
/// Flip to `false` when WanderMood opens multi-city Explore again.
const bool kLockExploreCityToRotterdam = true;
