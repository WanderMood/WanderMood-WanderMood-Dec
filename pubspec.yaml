name: wandermood
description: Een AI-gedreven reisapplicatie die reisaanbevelingen personaliseert op basis van gebruikersstemmingen.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# 1.0.0 is closed in App Store Connect; new submissions need a higher
# CFBundleShortVersionString (1.0.0 → 1.0.1). Build number must stay unique.
version: 1.0.1+51

environment:
  sdk: '>=3.2.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # Core
  supabase_flutter: ^2.9.1  # Updated for important fixes
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.5
  flutter_dotenv: ^5.1.0
  go_router: ^15.1.1
  app_links: ^6.4.1
  rive: ^0.13.20
  
  # UI/UX
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  table_calendar: ^3.2.0
  fl_chart: ^0.71.0
  flutter_svg: ^2.1.0
  lottie: ^3.3.1
  animated_text_kit: ^4.2.3
  confetti: ^0.8.0
  
  # Features
  geolocator: ^14.0.0
  geocoding: ^3.0.0
  weather: ^3.1.1
  flutter_google_maps_webservices: ^1.1.1
  google_maps_flutter: ^2.12.1
  map_launcher: ^3.5.0
  url_launcher: ^6.3.2
  share_plus: ^11.0.0
  webview_flutter: ^4.4.2
  
  # Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.5.3
  
  # Utils
  json_annotation: ^4.8.1
  freezed_annotation: ^2.4.1
  logger: ^2.0.2+1
  intl: ^0.20.2
  path_provider: ^2.1.5
  uuid: ^4.3.3
  http: ^1.4.0  # Updated for security improvements
  dio: ^5.4.0  # For API calls (OpenAI, etc.)
  crypto: ^3.0.3
  speech_to_text: ^7.0.0
  permission_handler: ^12.0.0+1
  flutter_tts: ^4.2.2
  just_audio: ^0.10.1
  
  # Notifications
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4

  # Phase 4C Dependencies
  connectivity_plus: ^6.0.5
  shimmer: ^3.0.0
  
  # Social Sign-In
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^7.0.1
  font_awesome_flutter: ^10.7.0
  
  # API Integration
  dart_openai: ^5.1.0
  image_picker: ^1.0.7
  qr_flutter: ^4.1.0
  flutter_staggered_grid_view: ^0.7.0
  google_places_flutter: ^2.1.1
  mobile_scanner: ^7.2.0
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  flutter_timezone: ^5.0.2
  tutorial_coach_mark: ^1.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Testing
  mocktail: ^1.0.3
  
  # Code generation
  build_runner: ^2.4.8
  json_serializable: ^6.9.0
  freezed: ^2.4.6
  riverpod_generator: ^2.6.3
  
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  source_gen: ^2.0.0
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Internationalization (i18n) configuration
  generate: true

  assets:
    - assets/icons/app_icon.png
    - assets/images/
    - assets/images/fallbacks/
    - assets/images/weather_icons/
    - assets/animations/
    - assets/sounds/
    - assets/globe/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic

  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic

# App icon master (1024×1024 PNG). Replace assets/icons/app_icon.png, then run:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    background_color: "#1a3d2e"
    theme_color: "#2A6049"
  macos:
    generate: true
  image_path: assets/icons/app_icon.png
  min_sdk_android: 21
  remove_alpha_ios: true
